# 🔄 Environment Variables Flow Diagram

## Build Time vs Runtime

```
┌─────────────────────────────────────────────────────────────────┐
│                        DOCKER BUILD                              │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Dockerfile                                              │    │
│  │                                                         │    │
│  │ ENV JAVA_HOME=/usr/lib/jvm/default-java    ◄─────────┐ │    │
│  │ ENV PATH="${PATH}:/usr/lib/jvm/..."        │ Build   │ │    │
│  │ ENV PYTHONUNBUFFERED=1                     │ Time    │ │    │
│  │                                            │ Only    │ │    │
│  │ RUN python download_models.py  ◄───────────┘         │ │    │
│  │   ^ Uses JAVA_HOME, PYTHONUNBUFFERED                 │ │    │
│  │                                                       │ │    │
│  │ ENV MLFLOW_URI=http://host.docker.internal:5000 ◄───┐│ │    │
│  │ ENV MODEL_NAME=codebuggy-detector           │ Build │ │    │
│  │ ENV MODEL_STAGE="Version 3"                 │ Time  │ │    │
│  │ ENV PORT=8080                               │ +     │ │    │
│  │                                             │ Runtime│ │    │
│  └─────────────────────────────────────────────┴───────┘ │    │
│                                                            │    │
│                          ▼                                 │    │
│                   Docker Image                             │    │
│         (All ENV variables baked in)                       │    │
└────────────────────────────────────────────────────────────┘
                           │
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DOCKER RUN / COMPOSE                        │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ docker-compose.yml                                      │    │
│  │                                                         │    │
│  │ environment:                                            │    │
│  │   - MLFLOW_URI=http://mlflow:5000  ◄─────────────┐    │    │
│  │   - MODEL_STAGE=Version 3                        │    │    │
│  │                                                   │    │    │
│  │ OR                                                │    │    │
│  │                                                   │    │    │
│  │ docker run -e MLFLOW_URI=http://test:5000 ...    │    │    │
│  │                                                   │    │    │
│  │                                          Runtime  │    │    │
│  │                                          Override │    │    │
│  └──────────────────────────────────────────────────┘    │    │
│                                                            │    │
│                          ▼                                 │    │
│                   Running Container                        │    │
│                                                            │    │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ Final Environment Variables:                           │    │
│  │                                                         │    │
│  │ JAVA_HOME=/usr/lib/jvm/default-java  (from Dockerfile)│    │
│  │ PATH=...:/usr/lib/jvm/.../bin        (from Dockerfile)│    │
│  │ PYTHONUNBUFFERED=1                   (from Dockerfile)│    │
│  │ MLFLOW_URI=http://mlflow:5000        (from compose)   │    │
│  │ MODEL_NAME=codebuggy-detector        (from Dockerfile)│    │
│  │ MODEL_STAGE=Version 3                (from compose)   │    │
│  │ PORT=8080                            (from Dockerfile)│    │
│  └────────────────────────────────────────────────────────┘    │
│                                                            │    │
│                          ▼                                 │    │
│                      app.py reads                          │    │
│                                                            │    │
│  ┌────────────────────────────────────────────────────────┐    │
│  │ mlflow_uri = os.getenv("MLFLOW_URI")                  │    │
│  │ model_name = os.getenv("MODEL_NAME")                  │    │
│  │ model_stage = os.getenv("MODEL_STAGE")                │    │
│  │ port = os.getenv("PORT")                              │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

---

## Variable Lifecycle

```
┌──────────────┐
│ Dockerfile   │
│ ENV VAR=val  │
└──────┬───────┘
       │
       │ docker build
       ▼
┌──────────────┐
│ Docker Image │ ◄─── Variables baked into image
│ VAR=val      │
└──────┬───────┘
       │
       │ docker run -e VAR=override
       │ OR docker-compose environment
       ▼
┌──────────────┐
│  Container   │ ◄─── Runtime override (if provided)
│ VAR=override │      Otherwise uses image default
└──────┬───────┘
       │
       │ Application reads
       ▼
┌──────────────┐
│   app.py     │
│ os.getenv()  │
└──────────────┘
```

---

## Precedence Order

```
Highest Priority
    ↓
┌─────────────────────────────────────┐
│ 1. docker run -e VAR=value          │ ◄─── Command line
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 2. docker-compose.yml environment   │ ◄─── Compose file
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 3. docker-compose.yml env_file      │ ◄─── .env file
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 4. Dockerfile ENV                   │ ◄─── Image default
└─────────────────────────────────────┘
    ↓
Lowest Priority
```

---

## Example: MLFLOW_URI Override

```
Step 1: Dockerfile
┌────────────────────────────────────────┐
│ ENV MLFLOW_URI=http://host.docker...   │
└────────────────────────────────────────┘
         MLFLOW_URI = "http://host.docker.internal:5000"

Step 2: docker-compose.yml
┌────────────────────────────────────────┐
│ environment:                           │
│   - MLFLOW_URI=http://mlflow:5000     │
└────────────────────────────────────────┘
         MLFLOW_URI = "http://mlflow:5000" ✅ (overrides Dockerfile)

Step 3: docker run -e
┌────────────────────────────────────────┐
│ docker run -e MLFLOW_URI=http://test  │
└────────────────────────────────────────┘
         MLFLOW_URI = "http://test" ✅ (overrides compose)

Final Value in Container:
┌────────────────────────────────────────┐
│ MLFLOW_URI = "http://test"            │
└────────────────────────────────────────┘
```

---

## Build-Time vs Runtime Variables

```
┌─────────────────────────────────────────────────────────────┐
│                    BUILD TIME                                │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Used during: docker build                                   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ JAVA_HOME        ◄─── Used by GumTree during build   │  │
│  │ PATH             ◄─── Used by system commands        │  │
│  │ PYTHONUNBUFFERED ◄─── Used by download_models.py     │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  These are BAKED INTO IMAGE                                 │
│  Cannot be changed without rebuilding                       │
│                                                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    RUNTIME                                   │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Used during: docker run / docker-compose up                │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ MLFLOW_URI   ◄─── Used by app.py at startup          │  │
│  │ MODEL_NAME   ◄─── Used by inference.py               │  │
│  │ MODEL_STAGE  ◄─── Used by inference.py               │  │
│  │ PORT         ◄─── Used by Flask                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  These can be OVERRIDDEN at runtime                         │
│  No rebuild needed                                          │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## When to Use Each

```
┌─────────────────────────────────────────────────────────────┐
│ Use Dockerfile ENV when:                                     │
├─────────────────────────────────────────────────────────────┤
│ ✅ Value is same for all environments                       │
│ ✅ Value is needed during build (JAVA_HOME, PATH)           │
│ ✅ You want sensible defaults                               │
│                                                              │
│ Examples:                                                    │
│   ENV JAVA_HOME=/usr/lib/jvm/default-java                   │
│   ENV PYTHONUNBUFFERED=1                                     │
│   ENV PORT=8080                                              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Use docker-compose environment when:                         │
├─────────────────────────────────────────────────────────────┤
│ ✅ Value differs between environments (dev/prod)            │
│ ✅ You want to override Dockerfile defaults                 │
│ ✅ Value is environment-specific                            │
│                                                              │
│ Examples:                                                    │
│   environment:                                               │
│     - MLFLOW_URI=http://mlflow:5000                         │
│     - MODEL_STAGE=Production                                │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Use docker run -e when:                                      │
├─────────────────────────────────────────────────────────────┤
│ ✅ Quick testing                                             │
│ ✅ One-off overrides                                         │
│ ✅ CI/CD pipelines                                           │
│                                                              │
│ Examples:                                                    │
│   docker run -e MODEL_STAGE=Staging ...                     │
│   docker run -e MLFLOW_URI=http://test:5000 ...            │
└─────────────────────────────────────────────────────────────┘
```

---

## Complete Flow Example

```
Developer writes Dockerfile:
┌────────────────────────────────────────┐
│ ENV MLFLOW_URI=http://localhost:5000   │
│ ENV MODEL_STAGE=Version 3              │
└────────────────────────────────────────┘
                ↓
        docker build
                ↓
┌────────────────────────────────────────┐
│ Image created with defaults            │
│ MLFLOW_URI=http://localhost:5000       │
│ MODEL_STAGE=Version 3                  │
└────────────────────────────────────────┘
                ↓
        docker-compose up
                ↓
┌────────────────────────────────────────┐
│ docker-compose.yml overrides:          │
│ MLFLOW_URI=http://mlflow:5000          │
│ (MODEL_STAGE stays Version 3)          │
└────────────────────────────────────────┘
                ↓
        Container starts
                ↓
┌────────────────────────────────────────┐
│ app.py reads:                          │
│ MLFLOW_URI=http://mlflow:5000 ✅       │
│ MODEL_STAGE=Version 3 ✅               │
└────────────────────────────────────────┘
                ↓
        Application runs
```

---

**Visual Guide Version**: 1.0  
**Last Updated**: 2026-01-18
